{
  "$schema": "../schemas/pack.schema.json",
  "schemaVersion": "2.0",
  "id": "react-fastapi-postgres",
  "kind": "stack",
  "name": "React + FastAPI + PostgreSQL",
  "description": "A second web golden stack: a React (Vite) single-page frontend served by a FastAPI backend, talking to PostgreSQL through SQLAlchemy/Alembic — materially different from nextjs-postgres (separate frontend/backend languages and frameworks, a real Alembic downgrade instead of a hand-written rollback script).",
  "version": "2.1.0",
  "inputs": [
    {
      "name": "projectName",
      "type": "string",
      "required": true,
      "description": "Package name used for both the frontend and backend manifests of the generated app."
    },
    {
      "name": "includeAuth",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Scaffold a starter session-cookie auth module."
    },
    {
      "name": "orm",
      "type": "enum",
      "required": true,
      "options": [
        "sqlalchemy"
      ],
      "description": "ORM used to talk to PostgreSQL."
    }
  ],
  "setup": [
    {
      "id": "frontend",
      "command": "npm create vite@latest frontend -- --template react-ts",
      "description": "Vite's own generator creates the React + TypeScript single-page frontend, unpinned so the template is current."
    },
    {
      "id": "backend",
      "command": "python -m venv backend/.venv && backend/.venv/bin/pip install fastapi uvicorn[standard] sqlalchemy alembic psycopg[binary] pydantic-settings",
      "description": "FastAPI has no project generator of its own, so the backend starts from a virtualenv and its dependency set; the lockfile produced from it is what pins versions."
    },
    {
      "id": "migrations",
      "command": "cd backend && .venv/bin/alembic init alembic",
      "description": "Alembic lays out its own migration tree. Chosen over a hand-written rollback script because it supports a real downgrade() natively."
    },
    {
      "id": "e2e",
      "command": "cd tests/e2e && npm install --save-dev @playwright/test @axe-core/playwright && npx playwright install --with-deps chromium",
      "description": "End-to-end suite against the built frontend served by the backend, which is how this stack is actually deployed."
    }
  ],
  "requiredArtifacts": [
    {
      "path": "backend/pyproject.toml",
      "description": "FastAPI backend manifest; the lock files beside it pin the resolved set."
    },
    {
      "path": "backend/app/models.py",
      "description": "SQLAlchemy models — the single source of truth for the data model."
    },
    {
      "path": "backend/alembic/versions",
      "description": "Migration history with real forward and backward paths."
    },
    {
      "path": "frontend/package.json",
      "description": "React/Vite frontend manifest."
    },
    {
      "path": "Dockerfile",
      "description": "Multi-stage production build (frontend build stage -> Python runtime) used for the deployment-package control."
    }
  ],
  "implementedBy": "reference-apps/react-fastapi-postgres-crud",
  "compatibility": {
    "requiresPacks": [],
    "conflictsWithPacks": [],
    "profiles": [
      "content",
      "internal-crud",
      "saas"
    ]
  },
  "verification": [
    {
      "id": "typecheck",
      "command": "cd backend && mypy app && cd ../frontend && npm run typecheck",
      "description": "Backend strict mypy and frontend TypeScript both compile with no errors."
    },
    {
      "id": "test",
      "command": "cd backend && pytest && cd ../tests/e2e && npm test",
      "description": "Backend unit/integration suite and frontend e2e suite both pass."
    },
    {
      "id": "build",
      "command": "docker build -t react-fastapi-postgres-crud .",
      "description": "Production multi-stage build succeeds, proving the build control."
    }
  ],
  "operationalEvidence": [
    {
      "control": "persistence",
      "evidence": "Alembic migrations under backend/alembic/versions/ give a repeatable, versioned persistence lifecycle."
    },
    {
      "control": "database-migration",
      "evidence": "Alembic supports a real downgrade() natively; the migration ships forward and backward paths together, rehearsed end to end."
    },
    {
      "control": "deployment-package",
      "evidence": "The multi-stage Dockerfile builds a pinned, reproducible production image that serves the built frontend directly from FastAPI."
    }
  ]
}
