{
  "$schema": "../schemas/pack.schema.json",
  "schemaVersion": "2.0",
  "id": "nextjs-postgres",
  "kind": "stack",
  "name": "Next.js + PostgreSQL",
  "description": "A full-stack web golden stack: Next.js app router frontend/API routes with a PostgreSQL database behind an ORM.",
  "version": "2.1.0",
  "inputs": [
    {
      "name": "projectName",
      "type": "string",
      "required": true,
      "description": "npm package name for the generated app."
    },
    {
      "name": "includeAuth",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Scaffold a starter session-based auth module."
    },
    {
      "name": "orm",
      "type": "enum",
      "required": true,
      "options": [
        "prisma",
        "drizzle"
      ],
      "description": "ORM used to talk to PostgreSQL."
    }
  ],
  "setup": [
    {
      "id": "scaffold",
      "command": "npx create-next-app@latest <projectName> --typescript --app --eslint --tailwind --no-src-dir --import-alias \"@/*\"",
      "description": "Next.js' own generator creates the project. Unpinned on purpose: the scaffold should be whatever the framework ships today, not whatever it shipped when this pack was written."
    },
    {
      "id": "orm",
      "command": "npm install @prisma/client @prisma/adapter-pg pg && npm install --save-dev prisma @types/pg && npx prisma@latest init --datasource-provider postgresql",
      "description": "Installs the ORM chosen by the `orm` input and lets its own CLI lay out prisma/. The app's dependencies are resolved at install time and pinned by the lockfile, which is where pinning belongs."
    },
    {
      "id": "e2e",
      "command": "npm install --save-dev @playwright/test @axe-core/playwright && npx playwright install --with-deps chromium",
      "description": "Playwright plus the accessibility matcher, needed by the end-to-end-tests and accessibility controls at R3."
    },
    {
      "id": "database",
      "command": "docker compose up -d db && npx prisma migrate dev --name init",
      "description": "Brings up PostgreSQL and applies the first migration, so persistence is real before any feature work starts."
    }
  ],
  "requiredArtifacts": [
    {
      "path": "package.json",
      "description": "Next.js app manifest; its lockfile is what pins the dependency set."
    },
    {
      "path": "prisma/schema.prisma",
      "description": "Database schema — the single source of truth for the data model."
    },
    {
      "path": "Dockerfile",
      "description": "Production build definition used for the deployment-package control. Authored, not emitted by any CLI, which is why it appears here and not in setup."
    },
    {
      "path": "docker-compose.yml",
      "description": "Local PostgreSQL and app services, and the target of the clean-environment rehearsal."
    }
  ],
  "implementedBy": "reference-apps/nextjs-postgres-crud",
  "compatibility": {
    "requiresPacks": [],
    "conflictsWithPacks": [],
    "profiles": [
      "content",
      "internal-crud",
      "saas"
    ]
  },
  "verification": [
    {
      "id": "typecheck",
      "command": "npm run typecheck",
      "description": "TypeScript compiles with no errors."
    },
    {
      "id": "test",
      "command": "npm test",
      "description": "Unit and integration test suite passes."
    },
    {
      "id": "build",
      "command": "npm run build",
      "description": "Production build succeeds, proving the build control."
    }
  ],
  "operationalEvidence": [
    {
      "control": "persistence",
      "evidence": "Prisma migrations under prisma/migrations/ give a repeatable, versioned persistence lifecycle."
    },
    {
      "control": "database-migration",
      "evidence": "The migrate script is idempotent and every forward migration ships with a documented rollback step."
    },
    {
      "control": "deployment-package",
      "evidence": "The Dockerfile builds a pinned, reproducible production image tagged with the build digest."
    }
  ]
}
