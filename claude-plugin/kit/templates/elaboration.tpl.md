---
intent: I-000
brief: open | fixed
spec: <docs/specs/F-xx-name/ — /spec กรอกตอนเริ่มเขียน spec>
researched: YYYY-MM-DD
---

# ข้อเสนอเพิ่มเติม: <ชื่อ intent>

> สร้างโดย `/elaborate` เก็บที่ `docs/elaboration/I-0xx-<slug>.md`
> ไฟล์นี้คือ **ข้อเสนอ** ไม่ใช่ requirement — ข้อไหนจะเข้า spec ขึ้นกับคอลัมน์ "การตัดสิน" เท่านั้น

## เป้าหมายจริงของสิ่งที่ขอ
<ลูกค้าต้องการทำอะไรให้ได้ หรือตัดสินใจเรื่องอะไร — ทุกข้อเสนอด้านล่างวัดกับย่อหน้านี้>

## สิ่งที่ระบบมีอยู่แล้วและเกี่ยวข้อง
- <โมดูล / ตาราง / spec เดิมที่ข้อเสนอต้องต่อด้วย พร้อม path>

## ข้อเสนอ

เรียงตามความสำคัญต่อเป้าหมาย · ระดับ: `must` / `should` / `could` ·
การตัดสิน: `pending` / `accepted` / `change-request` / `rejected` / `deferred` · ห้ามใช้เครื่องหมาย `|` ในช่อง

| ID | ข้อเสนอ | ทำไม (ผูกกับเป้าหมาย) | แหล่งที่มา | ระดับ | การตัดสิน |
|---|---|---|---|---|---|
| E-000 | <ตัวอย่าง: รายงานอายุหนี้ 30/60/90 วัน> | <ตัวอย่าง: ฝ่ายบัญชีต้องรู้ว่าตามหนี้รายไหนก่อน> | <URL ที่อ่านจริง หรือ path ใน repo หรือ "ความรู้ทั่วไปของโดเมน"> | must | pending |

- `accepted` → `/spec` เขียนเป็น requirement ที่อ้าง `E-xx`
- `change-request` → แจ้งลูกค้าเป็นคำขอเปลี่ยน scope ก่อน ยังไม่ทำ (ปกติของ brief แบบ `fixed`)
- `rejected` / `deferred` → ไม่ทำรอบนี้ · ปิดรอบแล้วยังเป็น `pending` ให้เปลี่ยนเป็น `deferred` ไม่ลบแถว

## แหล่งที่มาที่อ่าน
1. <ชื่อ — URL — อ่านเมื่อ YYYY-MM-DD>
